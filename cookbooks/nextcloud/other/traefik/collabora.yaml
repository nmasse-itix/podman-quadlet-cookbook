http:
  routers:
    collabora:
      rule: "Host(`collabora`)"
      entryPoints:
      - http
      #- https
      middlewares:
      service: "collabora"
      #tls:
      #  certResolver: le
  services:
    collabora:
      loadBalancer:
        servers:
        - url: "http://127.0.0.1:9980"
