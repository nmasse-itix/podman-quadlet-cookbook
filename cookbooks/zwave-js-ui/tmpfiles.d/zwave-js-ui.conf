# Precious data: the Z-Wave JS UI store (node names, associations, NVM backups,
# web interface users). The "$" marks it for removal on
# `systemd-tmpfiles --purge` (i.e. `make uninstall`).
d$ /var/lib/virtiofs/data/zwave-js-ui 0700 10035 10000 -
